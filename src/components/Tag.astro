---
interface Props {
    class?: string;
};

const { class: className } = Astro.props;
---

<span class={`flex gap-x-2 items-center rounded-full text-sm py-2 px-3 ${className}`}>
    <slot />
</span>